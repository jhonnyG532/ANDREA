version: '3.8'

services:
  cumple:
    build: .
    container_name: cumple-andrea
    ports:
      - "5000:5000"
    volumes:
      - ./static/uploads:/app/static/uploads
    environment:
      - FLASK_ENV=production
    restart: unless-stopped